<!-- pages/detail-video/detail-video.wxml -->
<video class="video" src="{{mvUrl}}" danmu-list="{{danmuList}}" referrer-policy="origin" />

<scroll-view class="content" scroll-y>
  <view class="info">
    <view>{{mvInfo.artistName}}</view>
  </view>
  <!-- <view class="related-video">
    <block wx:for="{{relatedVideo}}" :key="vid">
      <view class="item">
        <image class="item" src="{{item.coverUrl}}" mode="widthFix" />
        <view>{{item.title}}</view>
      </view>
    </block>
  </view> -->

  <view class="related-video">
    <block wx:for="{{100}}" wx:key="index">
      <view>{{item}}</view>
    </block>
  </view>
</scroll-view>

